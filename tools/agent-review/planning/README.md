Agent Review Planning

This directory is intentionally tracked in git and holds planning artifacts used for agent review bundles
(e.g., checklists, phase plans, validation notes). It is not runtime-generated output.
